[tool.poetry]
name = "learndnn"
version = "0.1.0"
description = ""
authors = ["murku"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pytest = "^7.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
